services:
    api:
        build: .
        depends_on:
            -   redis
            -   postgres
        volumes:
            -   .:/var/www
        ports:
            - "8000:8000"
            - "8080:8080"
            - "9004:9003"
        environment:
            - XDEBUG_MODE=debug

    postgres:
        image: postgis/postgis:15-master
        container_name: postgresql
        build: ./docker/database
        environment:
            POSTGRES_USER: ${POSTGRES_SUPERUSER:-postgres}
            POSTGRES_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD:-postgres}
            POSTGRES_DB: ${DB_DATABASE}
            DB_USERNAME: ${DB_USERNAME}
            DB_PASSWORD: ${DB_PASSWORD}
            DB_READ_USERNAME: ${DB_READ_USERNAME:-plusdate_app_read}
            DB_READ_PASSWORD: ${DB_READ_PASSWORD:-plusdate_read_password}
            DB_WRITE_USERNAME: ${DB_WRITE_USERNAME:-plusdate_app_write}
            DB_WRITE_PASSWORD: ${DB_WRITE_PASSWORD:-plusdate_write_password}
        volumes:
            - postgres_data:/var/lib/postgresql/data
        ports:
            - "5432:5432"

    redis:
        image: redis:7-alpine
        container_name: redis
        ports:
            - "6379:6379"
        volumes:
            - redis_data:/data

    minio:
        image: minio/minio:latest
        container_name: minio
        ports:
            - "9000:9000"
            - "9001:9001"
        volumes:
            - minio_data:/data
        environment:
            MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
            MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
        command: server /data --console-address ":9001"

volumes:
    postgres_data:
    redis_data:
    minio_data:
